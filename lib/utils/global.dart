import 'package:flutter/material.dart';

const kglobalpadding = EdgeInsets.symmetric(
  horizontal: 30.0,
  vertical: 25.0,
);


const kpaddingsymentric6 = EdgeInsets.symmetric(vertical: 6.0);
const kpaddingsymentric8 = EdgeInsets.symmetric(vertical: 8.0);
const kpaddingsymentric10 = EdgeInsets.symmetric(vertical: 10.0);
const kpaddingsymentric12 = EdgeInsets.symmetric(vertical: 12.0);
const kpaddingsymentric14 = EdgeInsets.symmetric(vertical: 14.0);
const kpaddingsymentric16 = EdgeInsets.symmetric(vertical: 66.0);

// Circular border

const kcircularborder16 = BorderRadius.only(
  topLeft: Radius.circular(16.0),
  topRight: Radius.circular(16.0),
);
